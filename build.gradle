// Root project: building the root triggers the Bukkit shadowJar.
// Fabric is included as a subproject but not wired up yet (build.gradle added later).

tasks.register('build') {
    group = 'build'
    description = 'Builds the Bukkit plugin jar.'
    dependsOn ':bukkit:shadowJar'
}
